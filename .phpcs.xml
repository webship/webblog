<?xml version="1.0" encoding="UTF-8"?>
<ruleset name="webshare">
  <arg name="extensions" value="inc,install,module,php,profile,test,theme"/>
  <description>PHP CodeSniffer configuration for Webshare module.</description>

  <!--Exclude folders-->
  <exclude-pattern>*/bower_components/*</exclude-pattern>
  <exclude-pattern>*/node_modules/*</exclude-pattern>
  <exclude-pattern>*/vendor/*</exclude-pattern>

  <file>.</file>

  <!-- Use PSR-12 coding standard with modifications -->
  <rule ref="PSR12">
    <!-- Allow Drupal-style method names (not strict camelCase) -->
    <exclude name="PSR1.Methods.CamelCapsMethodName.NotCamelCaps"/>
    <!-- Allow opening brace on same line for classes and functions (Drupal K&R style) -->
    <exclude name="PSR12.Classes.OpeningBraceSpace"/>
    <exclude name="PSR2.Classes.ClassDeclaration.OpenBraceNewLine"/>
    <exclude name="Squiz.Functions.MultiLineFunctionDeclaration.BraceOnSameLine"/>
    <!-- Allow both uppercase and lowercase for TRUE/FALSE/NULL constants -->
    <exclude name="Generic.PHP.LowerCaseConstant"/>
    <exclude name="Generic.PHP.UpperCaseConstant"/>
    <!-- Ignore line length warnings -->
    <exclude name="Generic.Files.LineLength"/>
  </rule>

  <!-- Generic sniffs -->
  <rule ref="Generic.Arrays.DisallowLongArraySyntax"/>
  <rule ref="Generic.Files.LineEndings"/>
  <rule ref="Generic.Formatting.SpaceAfterCast"/>
  <rule ref="Generic.NamingConventions.UpperCaseConstantName"/>
  <rule ref="Generic.PHP.LowerCaseKeyword"/>

  <!-- Use 2-space indentation (Drupal standard) instead of PSR-2's 4 spaces -->
  <rule ref="Generic.WhiteSpace.ScopeIndent">
    <properties>
      <property name="indent" value="2"/>
    </properties>
  </rule>

</ruleset>
